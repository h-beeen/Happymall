spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jpashop?user=hbyeon
    username: hbyeon
    password: 1q2w3e4r!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        defualt_batch_fetch_size: 100
        format_sql: true

decorator:
  datasource:
    p6spy:
      enable-logging: true
