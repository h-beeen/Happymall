#  mysql -u hbyeon -p -D jpashop
#  pw : 1q2w3e4r!

spring:
  profiles:
    default: dev
  datasource:
    url: jdbc:mysql://localhost:3306/jpashop?user=hbyeon
    username: hbyeon
    password: 1q2w3e4r!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        defualt_batch_fetch_size: 100

logging:
  level:
    org.hibernate.SQL: trace

decorator:
  datasource:
    p6spy:
      enable-logging: true
